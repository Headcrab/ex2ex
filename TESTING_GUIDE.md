# Инструкция по тестированию фильтрации строк

## 🎯 Цель тестирования

Проверить работу новой функциональности фильтрации строк при копировании диапазонов Excel.

## 📋 Подготовка

### 1. Убедитесь, что приложение запущено

**Вариант A: Docker**
```powershell
docker compose up -d
docker logs ex2ex-app
```

**Вариант B: Локально**
```powershell
.\ex2ex.exe
```

Откройте браузер: http://localhost:8080

### 2. Подготовьте тестовый Excel файл

Создайте файл `test.xlsx` с данными:

**Лист1:**
| A | B    | C         | D   | E    |
|---|------|-----------|-----|------|
| 1 | A123 | Product A | 100 | 2023 |
| 2 | B456 | Product B | 200 | 2024 |
| 3 | C123 | Product C | 150 | 2023 |
| 4 | D789 | Product D | 300 | 2024 |
| 5 | E333 | Product E | 250 | 2023 |

## 🧪 Тест 1: Фильтрация по маске "*3*"

### Шаги:

1. Откройте админ-панель: http://localhost:8080/admin
2. Создайте новое правило:
   - **Источник**: `Лист1!A1:E6`
   - **Назначение**: `Результат!A1`
   - **Столбец фильтрации**: `B`
   - **Маска фильтра**: `*3*`
3. Добавьте лист "Результат" с галочкой "Создать, если не существует"
4. Сохраните конфигурацию
5. Вернитесь на главную страницу
6. Загрузите тестовый файл
7. Скачайте результат

### Ожидаемый результат:

В листе "Результат" должны быть только строки с "3" в столбце B:
- Строка 1: A123
- Строка 3: C123
- Строка 5: E333

Всего 3 строки.

## 🧪 Тест 2: Фильтрация по началу строки "A*"

### Настройте правило:
- **Маска фильтра**: `A*`

### Ожидаемый результат:
Только строка 1 (A123)

## 🧪 Тест 3: Фильтрация по году "2023"

### Настройте правило:
- **Столбец фильтрации**: `E`
- **Маска фильтра**: `2023`

### Ожидаемый результат:
Строки 1, 3, 5 (где в столбце E значение "2023")

## 🧪 Тест 4: Копирование без фильтра

### Настройте правило:
- **Столбец фильтрации**: *(оставить пустым)*
- **Маска фильтра**: *(оставить пустым)*

### Ожидаемый результат:
Все 5 строк скопированы

## 🧪 Тест 5: Комбинированные правила

### Конфигурация:
```yaml
output_filename: combined_test.xlsx

mappings:
  # Правило 1: Фильтр по коду
  - source: "Лист1!A1:E6"
    destination: "Только с 3!A1"
    filter_column: "B"
    filter_mask: "*3*"
  
  # Правило 2: Без фильтра
  - source: "Лист1!A1:E6"
    destination: "Все данные!A1"
  
  # Правило 3: Фильтр по году
  - source: "Лист1!A1:E6"
    destination: "Только 2023!A1"
    filter_column: "E"
    filter_mask: "2023"

output_sheets:
  - name: "Только с 3"
    create_if_not_exists: true
  - name: "Все данные"
    create_if_not_exists: true
  - name: "Только 2023"
    create_if_not_exists: true
```

### Ожидаемый результат:
Три листа с разными наборами данных:
- **Только с 3**: 3 строки (A123, C123, E333)
- **Все данные**: 5 строк (все)
- **Только 2023**: 3 строки (1, 3, 5)

## ✅ Чек-лист проверки

- [ ] Фильтр `*3*` работает корректно
- [ ] Фильтр с началом строки `A*` работает
- [ ] Фильтр с точным совпадением работает
- [ ] Копирование без фильтра работает (обратная совместимость)
- [ ] Админ-панель сохраняет и загружает фильтры
- [ ] YAML редактор поддерживает новые поля
- [ ] Комбинация нескольких правил с разными фильтрами работает
- [ ] Пустые значения в столбце фильтрации пропускаются

## 🐛 Возможные проблемы

### Проблема: Все строки копируются, фильтр не работает

**Решение:**
1. Проверьте, что filter_column указан правильно (буква столбца: A, B, C...)
2. Убедитесь, что маска не пустая
3. Проверьте регистр символов (фильтр чувствителен к регистру)

### Проблема: Ни одна строка не копируется

**Решение:**
1. Проверьте, что маска соответствует данным в столбце
2. Попробуйте маску `*` (копирует непустые ячейки)
3. Проверьте логи: `docker logs ex2ex-app`

### Проблема: Конфигурация не сохраняется

**Решение:**
1. Проверьте, что config.yaml не read-only
2. В Docker убедитесь, что volume примонтирован без `:ro`
3. Проверьте права доступа к файлу

## 📊 Проверка производительности

Протестируйте на больших файлах:
- 100 строк
- 1000 строк
- 10000 строк

Замерьте время обработки и убедитесь, что разница минимальна.

## 📝 Отчёт о тестировании

После выполнения всех тестов заполните:

```
Дата тестирования: _______________
Тестировщик: _______________

Результаты:
[ ] Тест 1: Фильтрация *3* - ☐ PASS ☐ FAIL
[ ] Тест 2: Фильтрация A* - ☐ PASS ☐ FAIL
[ ] Тест 3: Фильтрация 2023 - ☐ PASS ☐ FAIL
[ ] Тест 4: Без фильтра - ☐ PASS ☐ FAIL
[ ] Тест 5: Комбинированные правила - ☐ PASS ☐ FAIL

Комментарии:
_________________________________
_________________________________
```

## 🎉 Готово!

Если все тесты прошли успешно, функциональность готова к использованию!
